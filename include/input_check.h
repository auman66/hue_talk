//
// Created by alex on 5/21/20.
//
#include <iostream>
#include <vector>
#include <string>

using namespace std;

#ifndef HUE_COMS_IP_CHECK_H
#define HUE_COMS_IP_CHECK_H

#endif //HUE_COMS_IP_CHECK_H

bool isNumber(const string& str);
vector<string> split(const string& str, char delim);
bool validateIP(string ip);
bool validatePort(string p);
bool validateUn(string un);
